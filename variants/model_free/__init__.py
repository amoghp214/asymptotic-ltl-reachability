"""
The model-free approach: the TDQL ladder of PAC stages.

Every variant here shares the same interface with the simulator -- reset to s0,
ask for the actions in the current state, sample an action in the current state
-- and none of them reads the transition function. See README.md in this folder
for what each file is, what it changed, and what that cost.
"""
